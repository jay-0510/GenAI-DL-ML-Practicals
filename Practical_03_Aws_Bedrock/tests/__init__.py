"""
tests package
=============
Empty on purpose — this file's only job is to make `tests/` importable as
a package so pytest and the project's import paths (`src.xxx`,
`utils.xxx`) resolve consistently whether tests are run from the repo root
or invoked via `python -m pytest`.
"""
